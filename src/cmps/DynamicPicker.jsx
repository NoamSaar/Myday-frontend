
export function DynamicPicker({ cmp, info, onUpdate }) {
    // switch (cmp) {
    //     case "status-picker":
    //         return <StatusPicker info={info} onUpdate={onUpdate} />;
    //     case "member-picker":
    //         return <MemberPicker info={info} onUpdate={onUpdate} />;
    //     default:
    //         return <p>UNKNOWN {cmp}</p>;
    // }
}
