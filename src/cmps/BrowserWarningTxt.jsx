
export function BrowserWarningTxt({ className }) {
    return (
        <p className={className}>
            ⚠️ You're using Samsung Internet, which doesn't support all site features. Please switch to another browser for full access.
        </p>
    )
}
